/// Match type enumeration
/// Defines the type of match: friendly, league, tournament
enum MatchType {
  /// Casual match with friends
  friendly,
  
  /// Competitive league match
  league,
  
  /// Tournament match
  tournament,
}
