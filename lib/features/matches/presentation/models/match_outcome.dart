/// UI-level enum representing the outcome of a match
/// 
/// This is used for presentation purposes only and does not modify
/// the domain layer. It allows friendly matches to display a draw state.
enum MatchOutcome {
  win,
  loss,
  draw,
}
